import XCTest
@testable import shiny_octo_barnacle

final class shiny_octo_barnacleTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(shiny_octo_barnacle().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
